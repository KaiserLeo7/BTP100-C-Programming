1. The advantage of using a structure as opposed to parallel arrays is that te structure can hold multiple data types.

2. The preferred method of declaring a struct data type is with a heading because the struct can then be used by other functions.